pre:
  - type: action|expo-init
    args:
      - yaml|app|name
      - env|TEMP_PATH

run: ~

post:
  - type: action|temp-prod
  - type: action|git-push
    args:
      - yaml|app|token
      - yaml|app|repo
      - env|OUTPUT_PATH
